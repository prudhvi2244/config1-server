eureka:
  instance:
    hostname: localhost
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/schools
    username: postgres
    password: 12345678
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8082
